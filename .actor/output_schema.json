{
  "actorOutputSchemaVersion": 1,
  "title": "OpenAPI MCP Server Compiler output",
  "description": "Generation summary in the dataset and the downloadable MCP server ZIP in the key-value store.",
  "properties": {
    "summary": {
      "type": "string",
      "title": "Run summary",
      "description": "Success status, statistics, warnings, and download link for the generated MCP server.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?view=summary"
    },
    "mcpServerZip": {
      "type": "string",
      "title": "MCP server ZIP",
      "description": "Generated deployable MCP server project archive ready to download and run.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/mcp-server.zip"
    },
    "warnings": {
      "type": "string",
      "title": "Warnings",
      "description": "Non-fatal generation warnings with error codes and locations.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?view=warnings"
    },
    "errors": {
      "type": "string",
      "title": "Errors",
      "description": "Fatal errors that prevented MCP server generation.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?view=errors"
    }
  }
}
